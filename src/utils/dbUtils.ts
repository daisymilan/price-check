/**
 * Database utilities — re-exported for any code that still imports from here.
 * The live data is now loaded asynchronously via DataContext.
 */

export { useData } from '../context/DataContext';
